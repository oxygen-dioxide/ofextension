{
	"name": "ofextension",
	"displayName": "OFextension",
	"description": "OpenFOAM求解器插件。可自动配置includePath（基于C/C++插件），实现OpenFOAM求解代码跳转、悬停提示；可自动配置调试相关选项，用于gdb调试代码。",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.55.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:ofextension.helloWorld",
		"onCommand:ofextension.ofInit"
	],
	"main": "./src/extension.js",
	"contributes": {
		"configuration": {
			"type": "object",
			"title": "OFextension",
			"properties": {
				"ofextension.OFpath": {
					"type": "string",
					"default": "/opt/openfoam8",
					"description": "OpenFOAM安装目录"
				},
				"ofextension.OFsrc": {
					"type":"string",
					"default": "/opt/openfoam8/src",
					"description": "OpenFOAM的src路径。默认路径一般是只读的，可以拷贝一份到用户目录，然后在此处指定，方便自定义源码或给源码注释等。"
				},
				"ofextension.GDBpath": {
					"type": "string",
					"default": "/usr/bin/gdb",
					"description": "指定gdb的路径。默认使用系统路径下的gdb，如果想使用自己编译的版本，可以自定义该路径。"
				}
			}
		},
		"commands": [
			{
				"command": "ofextension.helloWorld",
				"title": "Hello World"
			},
			{
				"command": "ofextension.ofInit",
				"title": "ofInit"
			}
		]
	},
	"scripts": {
		"lint": "eslint .",
		"pretest": "npm run lint",
		"test": "node ./test/runTest.js"
	},
	"devDependencies": {
		"@types/vscode": "^1.55.0",
		"@types/glob": "^7.1.3",
		"@types/mocha": "^8.0.4",
		"@types/node": "^12.11.7",
		"eslint": "^7.19.0",
		"glob": "^7.1.6",
		"mocha": "^8.2.1",
		"typescript": "^4.1.3",
		"vscode-test": "^1.5.0"
	}
}
